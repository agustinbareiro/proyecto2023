from django.db import models

# Create your models here.


# class Noticia(models.Model):
#     titulo = models.CharField(max_length=150)
#     categoria_notica = models.ForeignKey(Categoria, on_delete=models.CASCADE)
